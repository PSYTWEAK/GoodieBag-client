import React from "react";

import Head from 'next/head';

export function Head({ }) {
    return <Head>
        <title>GoodieBag</title>
        <meta name="description" content="The Defi application for mass buying tokens" />
        <link rel="icon" href="/favicon.ico" />
    </Head>;
}
